# Vercel: set in Project > Settings > Environment Variables > Production, then redeploy.
# URI includes database credentials. Never commit .env.local or paste credentials into logs.
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@YOUR_CLUSTER.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=gather_demo
